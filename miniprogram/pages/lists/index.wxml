<view class="page" style="padding-top: {{navPaddingTop}}px;">
  <view class="brand">今日待办</view>
  <view class="page-subtitle">场景清单 · 生活、学习各管各的</view>
  <view class="intro card">
    每个清单代表一种生活场景。进入「生活」就只看锻炼、家务；进入「学习」就只看学习安排。在这里新建待办，会自动归到当前清单里。
  </view>

  <view class="section-title">我的场景</view>
  <view class="card list-card">
    <view
      wx:for="{{lists}}"
      wx:key="id"
      wx:for-item="item"
      class="list-row {{item.isInbox ? 'list-row--inbox' : ''}}"
      data-id="{{item.id}}"
      bindtap="onOpenList"
    >
      <view class="list-icon">{{item.isInbox ? '收' : '清'}}</view>
      <view class="list-copy">
        <text class="list-name">{{item.name}}</text>
        <text class="list-meta">{{item.openCount}} 项未完成 · 共 {{item.totalCount}} 项</text>
      </view>
      <text class="list-enter" aria-hidden="true">进入</text>
      <button
        wx:if="{{!item.isInbox}}"
        class="list-menu"
        data-id="{{item.id}}"
        catchtap="onListMenu"
        aria-label="管理清单 {{item.name}}"
      >
        ⋯
      </button>
    </view>
    <view wx:if="{{!loading && lists.length === 0}}" class="empty">
      暂无清单
    </view>
  </view>

  <view class="section-title">新建场景清单</view>
  <view class="card create-list">
    <input
      value="{{newName}}"
      placeholder="例如：生活、学习、工作"
      maxlength="20"
      aria-label="新清单名称"
      bindinput="onNameInput"
      bindconfirm="onCreate"
    />
    <button class="ghost-button" bindtap="onCreate">创建</button>
  </view>
</view>
